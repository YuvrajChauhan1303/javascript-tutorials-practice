"use strict" //treats all JS code for the newer version
// nowadays, its implicitly used

/* 

    We are using NodeJS environment, instead of Browser environment. 

    Prioritize code readability!

*/

let name = "Yuvraj"; //String
let age = 20; //Number
let isMale = true; //Boolean

/*

    Other Data Types:
    -> null (standalone value):
        empty value
    -> bigint
    -> undefined :
        when value is not defined
    -> symbol :
        used for uniquen components (primarily in reactJS)
    -> object :

*/

console.log(typeof "Yuvraj");
console.log(typeof null); // null is a type of object
console.log(typeof undefined); // type is undefined
