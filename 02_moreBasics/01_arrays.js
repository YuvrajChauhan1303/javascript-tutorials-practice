const myArr = [1,2,3,4,5]; 

console.log(myArr[3]); 

// Indexing like most languages start from 0 to n-1 where n is the size!

// Array copies are shallow copies! That is they share the same reference point!

